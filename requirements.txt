# Core data + async
pandas>=2.0.0
pandas-market-calendars>=4.0  # NYSE holiday calendar for replay-harness trading-day enumeration (M2.2 sub-task #24)
numpy>=1.24.0
pyyaml>=6.0
websockets>=12.0
aiohttp>=3.9.0
lxml>=5.0.0            # required by pandas.read_html in watchlist_builder.py (Phase B)

# API SDKs
anthropic>=0.39.0      # claude-haiku-4-5 sentiment scoring
databento>=0.50.0      # ES futures order book (optional - skip if futures.enabled=false)
schwab-py>=1.5.1        # Schwab Trader API: option chains (/chains),
                        # NASDAQ_BOOK streaming. Pulls flask+werkzeug
                        # for the OAuth callback server - see the Rule 22
                        # audit note in main.py setup_logging before
                        # changing the logger suppression list.

httpx>=0.28.1          # imported DIRECTLY by data/alpaca_rest.py, not only
                       # transitively via schwab-py. Undeclared until 2026-08-17.
                       # Rule 22: httpx logs full request URLs at INFO, which is
                       # why log_hygiene.py pins it to WARNING.
mcp>=2.0.0             # MCP SDK used by mcp_server/server.py. The local package
                       # is named mcp_server precisely because a local mcp/
                       # directory would shadow this one (verified 2026-08-16).

# Standard library zoneinfo backport for Windows
tzdata>=2024.1
