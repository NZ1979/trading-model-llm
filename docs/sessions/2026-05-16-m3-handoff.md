Resuming M3 engine fixes (next session). Last commit on origin/main:
dabc4af (run `git log -1 --oneline` to confirm). Prior chat completed
two rounds of diagnosis. Findings + line-numbered defects in
docs/sessions/2026-05-16-m3-diagnosis.md (round 2 section at bottom).

CORE VERDICT: LLM signal engine is functionally working, not
mode-collapsed. The 1-trade outcome from the first M3 replay reflects
(a) prompt-imposed conservative calibration (intentional, in
prompts.py:36-43) and (b) universe choice (mega-caps in a quiet
5-day window). Five concrete defects to fix before any re-run will
produce clean signal-quality data.

DEFECT INVENTORY (all line-numbered in the diagnosis doc):

  #1: Reasoning truncated to 280 chars mid-word.
      strategy/llm/types.py:132 (Field max_length=280),
      strategy/llm/types.py:268-273 (validator truncates to 277+"..."),
      strategy/llm/prompts.py:65 (soft instruction).
      Widen all three together. Suggested target 800-1200 chars.

  #2: T1 schema_invalid failures (XML tags leak into string fields;
      stringified-JSON in concerns field). 2 of 1,742 in the run.
      Fix in strategy/llm/types.py via two new mode="before" validators:
      strip </xml> tag patterns on string fields; JSON-decode
      stringified-list values on concerns (logic already exists at
      strategy/llm/clients.py:337-363 for Qwen path — port it).

  #3: raw_response + risk_check_result not persisted to DB.
      data/replay/persistence.py:425-444 (and 376-395 for T3 path)
      omits two existing columns from the INSERT statement. The
      Anthropic client already populates raw_response at
      clients.py:218-229. Just add the columns to the INSERT.
      Add a sidecar column or JSON for LLMContext payload (separate
      small change).

  #4: Error-path discards raw input on validation failure.
      strategy/llm/clients.py:231-234 catches ValidationError but
      throws away tool_block.input. Capture it before raising,
      surface through the exception so the synthetic-Hold row gets
      the original malformed input in raw_response.

  #5: ** DECISION POINT, NOT A STRAIGHTFORWARD FIX ** Prompt
      aggressively biases toward Hold:
        - "conservative, high-conviction" (prompts.py:36)
        - "Hold is the default" (prompts.py:40)
        - "Confidence below 40 should always be Hold.
           Low-conviction trades lose money historically."
           (prompts.py:42-43)
      This is the cause of the 52-confidence ceiling and the
      1,427/1,742 calls at conf<30. Following instructions, not
      a bug. BUT: if the next M3 run is meant to gather signal-
      quality data on directional calls, the anchor will produce
      the same 1-trade outcome on ANY universe. Plus the "low-
      conviction trades lose money historically" claim cites
      data this fork doesn't have (provenance unclear; possibly
      inherited from gap-and-go base stats).

      Needs a decision conversation with Neale BEFORE editing:
        (a) Keep as-is, accept that M3 entry-sequence runs will
            produce sparse directional data, and rely on volume
            of runs to accumulate enough.
        (b) Soften the anchor (drop the 40 floor; rephrase the
            "historically lose money" claim or remove it; keep
            "Hold is the default" framing).
        (c) Replace with a calibration that targets a specific
            Buy/Sell rate (e.g., "produce a Buy or Sell on
            roughly 5-10% of evaluated bars when momentum is
            clear").
        (d) Hybrid: keep the anchor for live trading; relax it
            for replay/research mode via a prompt_version flag.

  #6: setup_label freeform sprawl. Constrain to enum in
      strategy/llm/types.py:131. Low priority. Already known.

  #7: (Bonus) AnthropicClient less defensive than LocalClient.
      Worth a small back-port pass after #1-4 land. See round 2
      finding 6 in the diagnosis doc.

SUGGESTED ORDER OF OPERATIONS:

  1. Open the session by verifying anchors per Rule 25.

  2. Ask Neale to decide on #5 (the four options above) BEFORE
     touching code. The answer changes the prompt edit and
     determines whether a `prompt_version` bump is needed.

  3. Land #1 (reasoning cap widening), #2 (two new validators),
     and #4 (capture raw input on error) in the same commit.
     These are all in strategy/llm/ and touch the LLM contract.
     Bump prompt_version in config/settings.yaml since the
     reasoning length is signaled in the system prompt.

  4. Land #3 (persistence INSERT fix) in a separate commit since
     it's a different module (data/replay/persistence.py).

  5. Land #5 prompt edit per Neale's decision in a separate
     commit. Bump prompt_version again if the system prompt text
     changes (same prompt_version invalidates ALL cached
     responses; bumping per-change keeps the cache invalidation
     story clean).

  6. Run pytest after each commit. Existing test suite is 930
     passing per the prior session.

  7. ONLY AFTER all of the above land + tests pass + Rule 27
     wrap clears: decide on the next replay run universe and
     scope.

NOT IN SCOPE for the fix chat:
  - Sentiment fixture rebuild (Rule 26 partition).
  - The replay re-run itself (premature until all fixes ship).
  - Defect #6 (setup_label enum) and #7 (LocalClient back-port).
    Both low priority; do them only if there's bandwidth after
    the core fixes land.

Read in order: CLAUDE.md (anchors per Rule 25),
CLAUDE_PREFLIGHT.md Rules 24-27,
docs/sessions/2026-05-16-m3-diagnosis.md (full findings, both
rounds — line numbers and verbatim prompt text live in the round
2 section at the bottom),
docs/sessions/2026-05-16-m3-first-run.md (original M3 run
context, only if needed for background),
docs/LLM_SIGNAL_INTERFACE.md (contract spec).

Open by verifying anchors, then ASK Neale about defect #5 (the
four options above). Do not start editing until he picks.
