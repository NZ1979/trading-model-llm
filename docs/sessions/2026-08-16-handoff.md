# Handoff prompt — session of 2026-08-16

Produced at the end of the 2026-08-15 session. Assumes HEAD at `814e450`,
working tree clean, test suite 1056 green on Python 3.14.

Paste the block below as the opening message of the next session.

---

```
Read SESSION_RESUME_2026-08-15.md first, then SESSION_RESUME_2026-08-14.md.
Where they disagree about what to BUILD, the 08-15 file wins - it records a
scope correction. docs/FEED_SPEC_V4.md is still the reference for what each
vendor is good at, but its §9 build order is superseded. Ignore
SESSION_RESUME_2026-05-12.md entirely.

Rules 28 and 29 were added to CLAUDE_PREFLIGHT.md yesterday (lines 551 and
613). Read them. They were written because of four wrong inferences in one
session. Rule 29 in particular: one command per message, command is the last
thing in the message, nothing after it.

Verify session anchors per Rules 23 and 25 and state them inline:
  1. date && TZ=America/New_York date
  2. working directory C:\trading\LLM model
  3. workstation Godzilla

GODZILLA IS ON MOUNTAIN TIME. Do not extrapolate elapsed time between
measurements - measure it. That error was made three times yesterday.

NEVER claim a file has been written, or any other action completed, without
a tool result in the same message showing it happened. That error was made
twice yesterday - a command was issued against a file that had been declared
written and did not exist.

Run these four checks before proposing work, one command at a time, and
report results:

  w32tm /stripchart /computer:time.windows.com /samples:5 /dataonly
  w32tm /query /configuration
  python -m scripts.schwab_login --status
  git status

Context for each:
  - Clock: W32Time was Stopped/Manual, so the 08-14 /resync was a one-shot
    and the clock free-ran at ~41ppm. Fixed 08-15. Baseline 18.4ms at 13:31
    MT, falling. This is the 24h HOLD check - the Rule 14 re-run that the
    08-14 session skipped. Want single digits or small-and-stable. A NEGATIVE
    offset means it overshot and is oscillating.
  - Config: confirm SpecialPollInterval is still 256. An earlier
    "w32tm /config /update" silently clobbered this same key once already.
  - Schwab token expires 2026-08-21. Sunday re-auth is routine, do it today
    rather than hitting it mid-week.
  - Tree was clean at 814e450.

FIRST QUESTION, before any prioritisation:

  Is the trading platform actually running anywhere right now? Check whether
  main.py runs on Godzilla, on the Hetzner VPS at 5.161.199.155, or nowhere.
  PROJECT_BLUEPRINT §3 is marked stale and says the repo was dormant
  mid-May to mid-August. Do not assume it is live. The answer reorders
  everything below.

IF THE PLATFORM IS LIVE - do this before Monday's open:

  RVOL reconciliation. The RVOL threshold comes from Polygon (full-market,
  180d, P85, refreshed 08:30 ET) but the runtime numerator came from Alpaca
  on IEX-only from 2026-05-14 to 2026-08-14. That is a real numerator at
  5-15% of true against a full-market denominator, making the gate roughly
  7-20x stricter than the 5x it reads as. Monday is the first RTH session
  after the SIP flip, so the gate goes from near-never-firing back to its
  designed sensitivity in one step.

  The test: sum Alpaca SIP cumulative pre-market volume 04:00-09:30 ET for
  one liquid symbol on 2026-08-14, and compare against Polygon's cumulative
  pre-market volume for the identical symbol and window. Both have historical
  REST, so this runs fine on a Sunday with no live market. Agreement within a
  few percent means numerator and denominator are finally on the same footing.
  Disagreement means the gate is still mis-scaled and the gap is your
  correction factor.

  Also check whether config/pm_rvol_thresholds.json exists at all. A read
  attempt returned nothing yesterday. If absent, every lookup falls through
  to HARD_FALLBACK_THRESHOLD = 5.0 and the per-ticker Phase C work never
  reaches production.

THEN, or FIRST if the platform is not live - the actual build:

The goal is on-demand analysis: I ask you about a stock or an option chain,
you read live data and help me analyse it. Nothing autonomous, nothing
watching, nothing alerting. The tick daemon and tick corpus from 08-14 stay
on disk, dormant. Do not extend them.

1. mcp/server.py SKELETON, exposing get_health() ONLY. Register it in Claude
   Desktop's claude_desktop_config.json and verify it is callable from the
   desktop app AND from a Cowork session (locally-registered MCP servers are
   proxied to Cowork through the device bridge). DO NOT build five tools
   against an unproven transport. This de-risks two unknowns immediately:
   whether the mcp SDK installs on Python 3.14, and whether Claude Desktop
   picks the server up at all. Commit and push before moving on.

2. data/alpaca_rest.py - equity snapshot, bars, latest quote/trade, plus the
   options chain snapshot with greeks and IV. This is the biggest new piece:
   data/alpaca_market_data.py is WEBSOCKET-ONLY and there is no Alpaca REST
   client anywhere in this codebase.

   IMPORTANT: Algo Trader Plus already includes real-time OPRA options data.
   It is paid for and has never been called. alpaca-py auto-selects the opra
   feed when entitled. The options stream is msgpack-only and does not accept
   a "*" wildcard subscription.

3. analysis/option_walls.py - lift walls/flow/IV-skew out of
   scripts/fetch_option_chain.py into a tested module. Follow the shape of
   analysis/futures_walls.py (pure detection function, frozen dataclass,
   monitor wrapper) but DO NOT port its persistence/anti-spoofing layer -
   that exists because displayed size can be spoofed, and open interest is
   settled and cleared and cannot be.

Target tool surface: get_health, get_snapshot, get_bars, get_option_chain,
get_oi_change. data/chain_store.py already backs get_oi_change.

TWO FREE EXPERIMENTS worth 10 minutes, both may improve the data for $0:

  a) Check Profile > Streaming Quotes on schwab.com, and check your
     professional/non-professional subscriber status. Schwab support states
     API entitlement mirrors schwab.com entitlement. This may flip isDelayed
     with no code and no money.
  b) Call /marketdata/v1/quotes with ONE equity symbol and ONE OCC option
     symbol together, and compare "realtime" and "quoteType" per symbol.
     Equity NBBO/realtime:true + option NFL/realtime:false isolates it to
     OPRA on the account rather than the app tier.

  Yesterday's session concluded isDelayed was inherent to a retail Individual
  Developer app. That was WRONG - /chains takes an "entitlement" parameter
  documented for client classifications PP-PayingPro / NP-NonPro /
  PN-NonPayingPro. Schwab classifies the CLIENT, not the app.

Working style, please:
  - One command at a time. The message ENDS with the command. Nothing after
    it - no questions, no parallel topics, no "while that runs." Rule 29.
  - State the expected output BEFORE the command so a mismatch is visible
    the moment it lands. This caught three errors yesterday.
  - Test in a sandbox before anything touches my disk.
  - Never assert the current time or elapsed time without measuring it in
    the same message.
  - Never assert that a file was written without the tool result proving it.
  - Rule 28: "probably" is a tripwire. If you reach for it, stop and
    investigate rather than shipping the hedge.
  - Commit and push each logical unit before starting the next (Rule 27).
  - Rule 24: git and file verification run from PowerShell on Godzilla, never
    from the Cowork bash mount.

Open questions I would like answered if you can:
  - Is Schwab's open interest T+1 or T+2? Their support phrasing "delayed by
    a day" is ambiguous between "normal" and "a day behind normal." If T+2,
    I need a real OI source and Polygon Options Starter at $29/mo carries
    open_interest, greeks and IV - the 15-minute delay is irrelevant to an
    end-of-day figure.
  - Does the ClockSkewMonitor in data/feed_daemon.py actually fire above
    500ms? It has never fired outside a test, and it is the only thing that
    would catch a clock regression once I stop checking manually.

First task if there is appetite for it: write Rule 30, covering asserted
facts rather than hypotheses. Rule 28 governs hedged claims and fires on
"probably"; it cannot fire on a confident statement about something never
checked. Both of yesterday's file-not-written errors and all three time
errors were of that class.
```
